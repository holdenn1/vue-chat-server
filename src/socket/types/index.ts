export enum NotificationType {
  SEND_MESSAGE = 'send_message',
  REMOVE_CHAT = 'remove_chat',
  UPDATE_USER = 'update_user',
  UPDATE_MESSAGE = 'update_message',
  REMOVE_MESSAGE = 'remove_message'
}

export enum JwtFunctionType {
  DECODE = 'decode',
  VERIFY = 'verifyAsync',
}
